# PortaLink
PortaLinkA lightweight bridge between private resources and public links. Expose a local/private resource through a temporary public tunnel without first uploading the resource to a cloud storage service.
